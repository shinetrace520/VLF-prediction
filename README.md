The ML models selected by sequential backward selection (SBS), sequential forward selection (SFS), and genetic selection (GS) are illustred by the example code 
for KRR and RFR model. Other ML model like OLS and SVR can be trainded using the same code by substitut the ML models in corresponding positions in the code. The 
running results may be different from the article stated slightly because the optimal hyperparameters can be tuned, but the main conclusions in the article could be 
still verified.
